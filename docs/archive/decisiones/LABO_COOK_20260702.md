# Labo cook 2026-07-02 - evidencia y recovery seguro

Fecha: 2026-07-02
Estado: aceptada

## Branch y commit

- Branch de trabajo: `labo-cook-evidence-nav`.
- Branch base real de laboratorio: `labo`.
- Commit base usado: `da685f6` (`labo: sesion TB4 real Parte C - fixes nav + mapa + contexto`).
- Commit de estos cambios: `labo: add evidence kit and safer obstacle recovery`.

## Problemas reales atacados

En el laboratorio el robot se trababa ante una silla/patas:

- avanzaba hacia el obstaculo;
- detectaba algo adelante;
- retrocedia;
- dejaba de ver las patas;
- volvia a intentar el mismo paso;
- repetia el forward/back durante varios segundos antes de destrabarse.

La causa probable es que las patas aparecian como obstaculos finos o ruido
intermitente: al retroceder, el LIDAR dejaba de verlas y el plan global seguia
queriendo pasar por el mismo lugar.

## Scripts nuevos

- `scripts/lab_record_all.sh`: crea `results/labo_demo/<timestamp>/`, graba
  rosbag, corre logger vivo, guarda metadata y genera reporte al cortar.
- `scripts/lab_live_logger.py`: registra `events.jsonl`, poses, goals, estados,
  cmd_vel, detecciones, particlecloud, ultimo path y `summary.json`.
- `scripts/lab_make_report.py`: genera `summary.md`, `timeline.csv` y
  `map_overlay.png`; si no hay `matplotlib`, cae a `map_overlay.svg`.
- `scripts/lab_record_rviz.sh`: grabacion opcional de pantalla con `ffmpeg`; si
  no existe `ffmpeg` o no hay `DISPLAY`, avisa y sale sin romper.
- `scripts/smoke_chair_obstacle.sh`: harness sintetico para validar que un
  obstaculo frontal persistente no produzca loop forward/back indefinido.
- `scripts/lab_replay_rviz.sh`: reproduce `<run_dir>/rosbag` con `/clock`, abre
  RViz y levanta los markers de demo para reconstruir un video post-labo.
- `scripts/lab_viz_markers.py`: publica `/lab_viz/markers` con trayectoria,
  goal, ultimo path, texto de estados/debug y rayo de deteccion del cono.
- `rviz/lab_replay.rviz`: vista RViz dedicada para replay/video.

Scripts ajustados:

- `scripts/parte_c_env.sh`: si no hay conda, usa ROS Humble del sistema y permite
  elegir overlay con `WORKSPACE_SETUP`.
- `scripts/smoke_mission.sh`: usa `python3` para correr en maquinas sin alias
  `python`.

## Como grabar evidencia manana

Primero conectar al robot y prender sensores:

```bash
turtlebot_mode 0
ros2 service call /tb4_0/start_motor std_srvs/srv/Empty {}
ros2 service call /tb4_0/oakd/start_camera std_srvs/srv/Empty {}
ros2 topic hz /tb4_0/scan
```

Terminal 1 - evidencia:

```bash
./scripts/lab_record_all.sh tb4_0
```

Terminal 2 - navegacion:

```bash
ros2 launch maze_nav nav_tb4_live.launch.py \
  map_yaml:=maps/laberinto_lab_20260702.yaml ns:=tb4_0
```

Terminal 3 - mision:

```bash
ros2 launch maze_mission mission.launch.py \
  params_file:=$(pwd)/config/parte_c/real.yaml
```

Terminal 4 - detector:

```bash
ros2 launch maze_perception cone_detector.launch.py \
  params_file:=$(pwd)/config/parte_c/real.yaml
```

Terminal 5 - RViz:

```bash
rviz2 -d src/maze_nav/rviz/nav.rviz --ros-args -p use_sim_time:=false \
  --remap /tf:=/tb4_0/tf \
  --remap /tf_static:=/tb4_0/tf_static \
  --remap /scan:=/tb4_0/scan
```

Opcional, Terminal 6 - pantalla RViz:

```bash
./scripts/lab_record_rviz.sh results/labo_demo/<timestamp>
```

## Como generar un video para el informe

### A. Grabacion en vivo

```bash
./scripts/lab_record_all.sh tb4_0
./scripts/lab_record_rviz.sh results/labo_demo/<timestamp>
```

### B. Replay post-labo

El replay usa el rosbag de `results/labo_demo/<timestamp>/rosbag`, publica
`/clock` y abre RViz con `use_sim_time:=true`:

```bash
./scripts/lab_replay_rviz.sh results/labo_demo/<timestamp> tb4_0
./scripts/lab_record_rviz.sh results/labo_demo/<timestamp>
```

Para revisar despacio:

```bash
RATE=0.5 ./scripts/lab_replay_rviz.sh results/labo_demo/<timestamp> tb4_0
```

## Como postprocesar evidencia

Al cortar `lab_record_all.sh` con `Ctrl-C`, intenta correr automaticamente:

```bash
python3 scripts/lab_make_report.py results/labo_demo/<timestamp>
```

El directorio queda con:

- `rosbag/`: datos ROS crudos;
- `events.jsonl`: eventos estructurados;
- `poses.csv`, `goals.csv`, `states.csv`, `cmd_vel.csv`;
- `latest_path.csv` y `path_updates.csv`;
- `summary.json` y `summary.md`;
- `timeline.csv`;
- `map_overlay.png` o `map_overlay.svg`.

## Cambio de navegacion ante silla/obstaculo

En `maze_nav/navigator.py` se mantuvo la prioridad de seguridad:

- no se subieron velocidades;
- no se bajo `safety_stop`;
- los obstaculos dinamicos se mantienen durante el goal actual y solo se limpian
  al recibir un goal nuevo;
- ante clearance frontal menor que `safety_stop`, el robot entra a `RECOVERY` y
  frena, en vez de seguir avanzando si no logro registrar celdas nuevas;
- `RECOVERY` ahora tiene hold + backoff corto temporizado;
- si el obstaculo fino no queda bien registrado y el bloqueo frontal se repite,
  se marca una barrera dinamica chica delante del robot para forzar replanning;
- si tras `max_recovery_attempts` sigue bloqueado, publica `IDLE`, deja
  `cmd_vel=0` y reporta `blocked_max_recovery_attempts` en `/nav_debug`;
- si no hay plan de recuperacion, queda detenido con
  `blocked_no_recovery_plan`.

`/nav_debug` publica `std_msgs/String` con JSON:

- `state`, `reason`, `pose`, `goal`;
- `forward_clearance_m`, `safety_stop_m`;
- `recovery_attempts`, `max_recovery_attempts`, `front_blocked_hits`;
- `dyn_obstacle_cells`, `last_dyn_added`;
- `path_len`, `path_idx`, `last_plan_length_m`;
- `cmd.linear_x`, `cmd.angular_z`;
- `blocked_reason`.

## A* y clearance

No se porto `astar_clearance` de `final/ab-casa` ni se tocaron los defaults de
inflacion, porque en el laberinto real ya hubo que bajar la inflacion efectiva a
0.20 m para no sellar pasillos. El `navigator.py` actual ya penaliza en A* la
cercania a obstaculos para empujar el camino hacia zonas mas despejadas:

```text
prox = max(0, robot_radius + inflation + clearance_extra_m - distancia_a_obstaculo)
costo += prox * clearance_weight
```

Se parametrizo de forma equivalente al comportamiento anterior:

- `clearance_extra_m: 0.25`;
- `clearance_weight: 3.0`.

Cambiar esos valores queda pendiente para pruebas especificas; no conviene
subirlos a ciegas antes de mañana.

## Validacion

Comando pedido:

```bash
source /opt/ros/humble/setup.bash
colcon build --symlink-install
```

Resultado en esta maquina:

```text
Summary: 2 packages finished [3.90s]
2 packages failed: maze_nav maze_slam
1 package aborted: maze_mission
error: can't copy '/home/marchan/Robotica/tp_final_ws/build/maze_slam/launch/fastslam.launch.py':
doesn't exist or not a regular file
error: can't copy '/home/marchan/Robotica/tp_final_ws/build/maze_nav/launch/nav_base.launch.py':
doesn't exist or not a regular file
```

Interpretacion: fallo de entorno/estado generado viejo en `build/maze_slam`, no
de codigo fuente. Para no borrar `build/`, se valido con bases temporales:

```bash
source /opt/ros/humble/setup.bash
colcon --log-base /tmp/tp_final_ws_log_replay build --symlink-install \
  --build-base /tmp/tp_final_ws_build_replay \
  --install-base /tmp/tp_final_ws_install_replay
```

Resultado:

```text
Summary: 5 packages finished [30.6s]
```

Tests:

```bash
source /opt/ros/humble/setup.bash
source /tmp/tp_final_ws_install_replay/setup.bash
colcon --log-base /tmp/tp_final_ws_log_replay_test test \
  --build-base /tmp/tp_final_ws_build_replay \
  --install-base /tmp/tp_final_ws_install_replay \
  --test-result-base /tmp/tp_final_ws_build_replay \
  --packages-select maze_nav maze_mission maze_perception \
  --event-handlers console_direct+
colcon test-result --test-result-base /tmp/tp_final_ws_build_replay --verbose
```

Resultado:

```text
maze_perception: 10 passed
maze_mission: 40 passed
maze_nav: Ran 0 tests
Summary: 50 tests, 0 errors, 0 failures, 0 skipped
```

Validacion rapida de replay/video:

```bash
python3 -m py_compile \
  scripts/lab_live_logger.py \
  scripts/lab_make_report.py \
  scripts/lab_viz_markers.py \
  src/maze_nav/maze_nav/navigator.py

bash -n \
  scripts/lab_record_all.sh \
  scripts/lab_record_rviz.sh \
  scripts/lab_replay_rviz.sh

./scripts/smoke_chair_obstacle.sh
```

Resultado:

```text
py_compile: OK
bash -n: OK
[smoke_chair_obstacle] OK
```

Tambien se corrio el comando exacto de tests sobre `install/` local:

```bash
source /opt/ros/humble/setup.bash
source install/setup.bash
colcon test --packages-select maze_nav maze_mission maze_perception --event-handlers console_direct+
```

Resultado parcial:

```text
maze_perception: 10 passed
maze_nav: Ran 0 tests
maze_mission: fallo por install/maze_mission/share/maze_mission/package.sh faltante
```

Ese faltante es consecuencia del build local abortado por `build/maze_slam`.

Smokes:

```bash
WORKSPACE_SETUP=/tmp/tp_final_ws_install_all/setup.bash ./scripts/smoke_mission.sh reachable 60
WORKSPACE_SETUP=/tmp/tp_final_ws_install_all/setup.bash ./scripts/smoke_mission.sh wall 60
./scripts/smoke_chair_obstacle.sh
```

Resultados:

```text
[monitor] RESULT: PASS       # reachable
[monitor] RESULT: PASS       # wall
[smoke_chair_obstacle] OK
```

Smoke de silla:

```text
[smoke_chair_obstacle] OK
cmd_linear=[0.0, -0.05, 0.0, -0.05, 0.0]
dyn_obstacle_cells=50
final_debug.reason=blocked_max_recovery_attempts
```

## Pendiente

- Probar con la silla real del laboratorio y ajustar solo si hace falta:
  `max_recovery_attempts`, `recovery_hold_s`, `recovery_backoff_s` y
  `front_obstacle_mark_radius`.
- Verificar que el bloqueo seguro no saltee waypoints utiles por falsos positivos
  de MCL cerca de paredes.
- El deadlock inicial de LOCALIZE descrito en `docs/CONTEXTO_LABO.md` no se
  resuelve aca; seguir usando giro/teleop inicial o atacarlo en una tarea aparte.
- Confirmar en informe si la corrida final demuestra esquive o, como minimo,
  safe stop defendible ante obstaculo no mapeado.
